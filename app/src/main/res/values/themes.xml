<resources>
    <!-- Base application theme. -->
    <style name="Theme.Talentara" parent="Theme.Material3.Light.NoActionBar">
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/blue</item>
        <item name="colorSecondaryContainer">@color/white</item>
    </style>
</resources>